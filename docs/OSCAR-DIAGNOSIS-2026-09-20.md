# فحص أوسكار وإزالة HiTV — 2026-09-20

> تحديث لاحق في اليوم نفسه: المستخدم أجاز توقيعاً جديداً ورفض الانتقال إلى أوسكار. أزيل زر تشغيل التطبيق الخارجي؛ تبقى معالجة الفشل وإعادة المحاولة داخل عالم المصادر. أزيل اسم Oscar TV الظاهر في إعدادات القسم. المفتاح الجديد يحل عائق تجهيز APK موقع لتثبيت جديد، ولا يحل رفض API. التفاصيل التالية توثق تشخيص الجولة السابقة؛ وصف زر أوسكار والمفتاح المفقود فيها تاريخي.

## النسخة المعتمدة لهذه الجولة

- الملف: `AWR-World-Sources-season-restore.apk` المرفق من المستخدم، وليس نسخة اختبار سبتمبر 19.
- SHA-256: `85aca2be533a5905801b6633c94939e5f32ea88df28be1124298f5edcfdabd51`.
- شهادة التوقيع SHA-256: `3586142178e22cb6193aec74422ea8b04c7e8300117a680118a554aa7421ce3e`.
- لم نعثر على مفتاحها الخاص. لا يمكن استعادة مفتاح التوقيع الخاص من APK.
- مصدر المقارنة: Oscar TV 1.1.4، الحزمة `com.drama.mp4`، SHA-256: `e15d2de82257e55a5ea7ffef7a78ec205caf0c02ddfa7b80673dba68006226d7`.

## نتائج الفحص

تم تتبع ApiService، إنشاء Retrofit/عميل HTTP، سلسلة اعتراض الطلبات، إعداد النطاق، نقطة التوقيع الأصلية، ومسار تشغيل التطبيق من manifest. لم نفحص كل تعليمة داخل جميع مكتبات الإعلان والطرف الثالث، ولم نختبر تشغيل المحتوى على هاتف.

1. عناوين API ومعامل `app_version=14` في الربط الحالي توافق النسخة الأصلية. خريطة المسارات موجودة في `artifacts/oscar-api-map.json`.
2. العميل الأصلي يضيف للطلبات التي تبدأ بـ `/api/` حقول `X-Iron-Sig` و`X-Iron-Ts` و`X-Iron-Nonce` و`X-Iron-Diag`. التوقيع يأتي من مكتبة `iron_fingerprint` الأصلية، ويستقبل سياق التطبيق واسم حزمته والمسار والوقت وقيمة عشوائية. هذا عقد توثيق طلبات، وليس مجرد رابط API ناقص أو إعداد MX.
3. التطبيق الأصلي يحتوي أيضاً على فحص سلامة الحزمة/الشهادة، وتصحيح الوقت من رأس Date، وتحديث النطاق من إعداداته. بياناته الافتراضية في الملف المرفق تشير إلى `https://ostvapp.cam/`. لم ننقل مفاتيح أو مكتبة توقيع أو نحاكِ هوية التطبيق الأصلي.
4. طلب حقيقي بنفس إعدادات الربط الحالي إلى `api/v2/home.php?app_version=14` في هذه الجولة أعاد HTTP 403، بنوع `application/json` وجسم `{"status":"error","message":"Forbidden"}`. الدليل في `artifacts/oscar-live-diagnostic.json`. هذا يختلف عن رد Cloudflare 1010 المسجل في الجولة السابقة.
5. الرد لا يكشف سبب الرفض التفصيلي من الخادم. غياب توثيق الطلبات فرق مؤكد في التنفيذ ومتسق مع الرفض؛ لا نستطيع إثبات أنه السبب الوحيد دون وصول معتمد أو سجلات الخادم. لم نثبت أن الكتالوج أو المشاهدة يعملان داخل الربط.
6. المشغل الأصلي يفضل روابط المصدر الخاصة وقد يفتح TDM. تبديل المشغل إلى MX لا يعالج رفض تحميل الكتالوج. مسارات MX والتحميل السابقة في نسخة المستخدم محفوظة.

## التعديل المنفذ

- حذف HiTV من شريط التبويبات الظاهر، مع إبقاء أرقام الوجهات القديمة ثابتة حتى لا تتحول ضغطة «عالم المصادر» إلى HiTV أو تتأثر صفحات الدراما. أصناف HiTV القديمة غير المستخدمة تبقى داخل DEX للحفاظ على التوافق؛ هذا حذف للقائمة كما طلب المستخدم.
- عرض رفض المصدر بوضوح بدلاً من اعتباره كتالوجاً فارغاً، مع إعادة المحاولة وزر اختياري «فتح أوسكار الأصلي».
- الزر يفتح نقطة التشغيل العامة المعلنة في التطبيق المرفق: `com.drama.mp4.ui.splash.SplashActivity`. لا يفتح نشاطاً داخلياً خاصاً، ولا يمرر بيانات حساب أو هوية مصطنعة. إذا لم يكن أوسكار مثبتاً تظهر رسالة تثبيته. الرجوع يبقي واجهة عالم المصادر متاحة.
- هذا الزر بديل خارجي عند تعذر الاتصال، **وليس إصلاحاً للدمج الكامل أو ضماناً لتشغيل المصدر**.

## الحفظ والتحقق

يقتصر الدمج في هذه الجولة على `WitcherTabs` و`OscarApi` و`OscarExperience` وأصنافها المولدة. تبقى Ui وOscarCatalog وOscarMedia وDramaActivity وإصلاح المواسم وكل الأصناف الأخرى من APK المستخدم كما هي، ويثبت تقرير الدمج تطابقها بعد كتابة DEX موحد. تبقى ملفات DEX الأخرى والموارد والمكتبات وmanifest كما هي.

اختبارات Robolectric تتحقق من اختفاء HiTV وثبات وجهات الدراما، ظهور رفض 403 وزر فتح المصدر الصحيح، عدم إغلاق واجهة عالم المصادر عند فتح أوسكار، والفلاتر/الصفحات/المواسم/رابط MX وحالات التشغيل الموجودة. هذه اختبارات محلية وليست إثباتاً لنجاح الخدمة الحية.

يمكن تجهيز حمولة APK والتحقق منها باستعمال `tooling/patch_apk.py --unsigned`. الناتج غير قابل للتثبيت حتى يوقّع بالمفتاح الموافق للشهادة أعلاه. لا تعوّض المفتاح المفقود بشهادة جديدة ثم تصف الملف بأنه تحديث متوافق.

## ما يلزم لإكمال الطلب

1. مفتاح توقيع النسخة المعتمدة (`.p12` أو `.jks`) لإصدار APK يثبت فوقها دون حذف التطبيق وبياناته.
2. API أو SDK أو وسيلة توثيق يتيحها صاحب أوسكار لهذا التطبيق، أو وصول معتمد لسجلات الخادم لتحديد الرفض ومعالجته. نسخ العناوين والواجهات لا يحقق هذا الشرط.

لا تصف الطلب بأنه مكتمل أو «عالم المصادر يعمل» قبل وجود اتصال كتالوج وتشغيل ناجحين فعلياً.
